@using Undersoft.SDK.Service.Access;

@inherits LayoutComponentBase
@layout GenericLayout

@typeparam TLogo where TLogo : Icon, new()
@typeparam TApps where TApps : class, IOrigin, IInnerProxy
@typeparam TNavMenu where TNavMenu : class, IOrigin, IInnerProxy
@typeparam TAccount where TAccount : class, IOrigin, IInnerProxy, IAuthorization
 
<GenericPresentingHeader Logo="@Logo" TNavMenu="TNavMenu" TAccount="TAccount" TAccount="TAccount" TValidator="TValidator" />

<CascadingValue Value="@Body">

    <GenericPresentingBody TNavMenu="TNavMenu" />

</CascadingValue>

<GenericPresentingSetFooter TApps="TApps" />

@code
{
    public TLogo? Logo { get; set; } = new();
}